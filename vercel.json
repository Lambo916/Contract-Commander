{
  "buildCommand": "mkdir -p dist && mkdir -p api/_server && cp -r public/* dist/ && npx esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outfile=api/_server/index.js --allow-overwrite",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/index"
    }
  ]
}
