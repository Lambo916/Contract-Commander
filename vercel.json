{
  "buildCommand": "mkdir -p dist api/_server && cp -r public/* dist/ && npx esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outfile=api/_server/index.js",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/index"
    }
  ]
}
