# YourBizGuru Toolkit Template - Environment Variables
# Copy this file to .env for local development (DO NOT commit .env to git)

# OpenAI API Key (Required)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Supabase Configuration (Required)
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api

# Backend Supabase (Node.js/Express)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key_here
DATABASE_URL=postgresql://postgres.your-project:password@aws-0-region.pooler.supabase.com:5432/postgres

# Frontend Supabase (Vite/Browser - prefixed with VITE_)
# OPTIONAL: The code automatically falls back to SUPABASE_URL/SUPABASE_ANON_KEY if these are not set
# Only set these if you need different values for browser vs server
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Development Settings
NODE_ENV=development
PORT=3000

# Production Notes:
# - For Replit: Add SUPABASE_URL, SUPABASE_ANON_KEY, DATABASE_URL, and OPENAI_API_KEY to Replit Secrets
#   VITE_* variables are OPTIONAL - the code automatically falls back to SUPABASE_* values
# - For Vercel: Add all variables to Vercel Project Settings > Environment Variables
# - Never commit actual API keys or database URLs to version control
# - VITE_* variables are exposed to browser - only use public/anonymous keys
# - The lib/supabase.ts pickEnv helper automatically tries multiple variable names for compatibility